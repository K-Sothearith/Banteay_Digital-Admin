import { useEffect, useState } from 'react';
import { api } from '../services/api';

const emptyTopic = { title: '', slug: '', category: '', shortDescription: '', content: '', warningSigns: [], preventionTips: [], indicators: [], relatedTopicIds: [], imageUrl: '', icon: '', isPublished: false };
const toLines = (items) => (items || []).join('\n');
const toList = (value) => value.split('\n').map((item) => item.trim()).filter(Boolean);
const dateLabel = (date) => new Intl.DateTimeFormat(undefined, { dateStyle: 'medium' }).format(new Date(date));

function TopicModal({ allTopics, topic, onClose, onSave }) {
  const [form, setForm] = useState(() => topic ? { ...topic, relatedTopicIds: topic.relatedTopics?.map((item) => item.id) || [] } : emptyTopic);
  const [error, setError] = useState('');
  const [saving, setSaving] = useState(false);
  const update = (key, value) => setForm((current) => ({ ...current, [key]: value }));
  const submit = async (event) => {
    event.preventDefault();
    const payload = { ...form, title: form.title.trim(), slug: form.slug.trim(), category: form.category.trim(), shortDescription: form.shortDescription.trim(), content: form.content.trim(), warningSigns: toList(form.warningSigns), preventionTips: toList(form.preventionTips), indicators: toList(form.indicators), imageUrl: form.imageUrl.trim() || null, icon: form.icon.trim() || null };
    if (!payload.title || !payload.slug || !payload.category || !payload.shortDescription || !payload.content || !payload.warningSigns.length || !payload.preventionTips.length || !payload.indicators.length) { setError('Complete all required fields and provide at least one item in each guidance list.'); return; }
    setSaving(true); setError('');
    try { await onSave(topic?.id, payload); onClose(); } catch (requestError) { setError(requestError.message || 'Could not save this knowledge item.'); } finally { setSaving(false); }
  };
  const fieldClass = 'w-full rounded-xl border border-slate-200 bg-white px-3.5 py-2.5 text-sm text-slate-900 outline-none focus:border-[#4b9efe] focus:ring-2 focus:ring-[#4b9efe]/20 dark:border-[#1e3568] dark:bg-[#101e40] dark:text-white';
  return <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/60 p-4 backdrop-blur-xs"><div className="flex max-h-[90vh] w-full max-w-3xl flex-col overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-2xl dark:border-[#1e3568] dark:bg-[#0c1733]"><div className="flex items-center justify-between border-b border-slate-100 px-6 py-4 dark:border-[#1e3568]/60"><div><h2 className="m-0 text-lg font-bold text-slate-900 dark:text-white">{topic ? 'Edit safety knowledge' : 'Create safety knowledge'}</h2><p className="mb-0 mt-1 text-xs text-slate-500 dark:text-slate-400">Published items are visible to community members.</p></div><button type="button" onClick={onClose} className="grid h-8 w-8 place-items-center rounded-xl text-slate-400 hover:bg-slate-100 dark:hover:bg-[#132248]"><i className="fa-solid fa-xmark" /></button></div><form onSubmit={submit} className="space-y-4 overflow-y-auto p-6"><div className="grid gap-4 sm:grid-cols-2"><label className="text-xs font-semibold uppercase tracking-wider text-slate-500 dark:text-slate-400">Title<input value={form.title} onChange={(event) => update('title', event.target.value)} className={`${fieldClass} mt-1.5 normal-case tracking-normal`} /></label><label className="text-xs font-semibold uppercase tracking-wider text-slate-500 dark:text-slate-400">Slug<input value={form.slug} onChange={(event) => update('slug', event.target.value)} placeholder="phishing-fake-links" className={`${fieldClass} mt-1.5 normal-case tracking-normal`} /></label><label className="text-xs font-semibold uppercase tracking-wider text-slate-500 dark:text-slate-400">Category<input value={form.category} onChange={(event) => update('category', event.target.value)} placeholder="Phishing" className={`${fieldClass} mt-1.5 normal-case tracking-normal`} /></label><label className="text-xs font-semibold uppercase tracking-wider text-slate-500 dark:text-slate-400">Icon <span className="normal-case tracking-normal">(optional)</span><input value={form.icon || ''} onChange={(event) => update('icon', event.target.value)} placeholder="shield" className={`${fieldClass} mt-1.5 normal-case tracking-normal`} /></label></div><label className="block text-xs font-semibold uppercase tracking-wider text-slate-500 dark:text-slate-400">Short description<textarea rows="2" value={form.shortDescription} onChange={(event) => update('shortDescription', event.target.value)} className={`${fieldClass} mt-1.5 resize-y normal-case tracking-normal`} /></label><label className="block text-xs font-semibold uppercase tracking-wider text-slate-500 dark:text-slate-400">Full content and examples<textarea rows="6" value={form.content} onChange={(event) => update('content', event.target.value)} className={`${fieldClass} mt-1.5 resize-y normal-case tracking-normal`} /></label><div className="grid gap-4 sm:grid-cols-3">{[['warningSigns', 'Warning signs'], ['preventionTips', 'Prevention tips'], ['indicators', 'Indicators / examples']].map(([key, label]) => <label key={key} className="block text-xs font-semibold uppercase tracking-wider text-slate-500 dark:text-slate-400">{label}<textarea rows="5" value={typeof form[key] === 'string' ? form[key] : toLines(form[key])} onChange={(event) => update(key, event.target.value)} placeholder="One item per line" className={`${fieldClass} mt-1.5 resize-y normal-case tracking-normal`} /></label>)}</div><label className="block text-xs font-semibold uppercase tracking-wider text-slate-500 dark:text-slate-400">Image URL <span className="normal-case tracking-normal">(optional)</span><input type="url" value={form.imageUrl || ''} onChange={(event) => update('imageUrl', event.target.value)} className={`${fieldClass} mt-1.5 normal-case tracking-normal`} /></label><fieldset><legend className="text-xs font-semibold uppercase tracking-wider text-slate-500 dark:text-slate-400">Related topics</legend><div className="mt-2 grid gap-2 sm:grid-cols-2">{allTopics.filter((item) => item.id !== topic?.id).map((item) => <label key={item.id} className="flex items-center gap-2 rounded-lg border border-slate-200 p-2 text-sm text-slate-700 dark:border-[#1e3568] dark:text-slate-200"><input type="checkbox" checked={form.relatedTopicIds.includes(item.id)} onChange={(event) => update('relatedTopicIds', event.target.checked ? [...form.relatedTopicIds, item.id] : form.relatedTopicIds.filter((id) => id !== item.id))} />{item.title}</label>)}</div></fieldset><label className="flex items-center gap-2 text-sm font-semibold text-slate-700 dark:text-slate-200"><input type="checkbox" checked={form.isPublished} onChange={(event) => update('isPublished', event.target.checked)} />Publish this item</label>{error ? <p className="m-0 text-sm font-semibold text-red-600" role="alert">{error}</p> : null}<div className="flex justify-end gap-3 border-t border-slate-100 pt-4 dark:border-[#1e3568]/60"><button type="button" onClick={onClose} className="rounded-xl border border-slate-200 px-4 py-2 text-xs font-semibold text-slate-700 dark:border-[#1e3568] dark:text-slate-300">Cancel</button><button disabled={saving} type="submit" className="rounded-xl bg-[#012475] px-5 py-2 text-xs font-bold text-white disabled:opacity-60 dark:bg-[#10b981]">{saving ? 'Saving…' : 'Save knowledge'}</button></div></form></div></div>;
}

export default function SafetyKnowledge() {
  const [topics, setTopics] = useState([]); const [loading, setLoading] = useState(true); const [error, setError] = useState(''); const [editing, setEditing] = useState(undefined); const [query, setQuery] = useState('');
  useEffect(() => { let active = true; api.admin.safetyKnowledge().then((response) => { if (active) setTopics(response.knowledge || []); }).catch((requestError) => { if (active) setError(requestError.message || 'Could not load Community Safety knowledge.'); }).finally(() => { if (active) setLoading(false); }); return () => { active = false; }; }, []);
  const load = async () => { setLoading(true); setError(''); try { const response = await api.admin.safetyKnowledge(); setTopics(response.knowledge || []); } catch (requestError) { setError(requestError.message || 'Could not load Community Safety knowledge.'); } finally { setLoading(false); } };
  const save = async (id, data) => { if (id) await api.admin.updateSafetyKnowledge(id, data); else await api.admin.createSafetyKnowledge(data); await load(); };
  const togglePublication = async (topic) => { try { await api.admin.publishSafetyKnowledge(topic.id, !topic.isPublished); await load(); } catch (requestError) { setError(requestError.message || 'Could not update publication status.'); } };
  const remove = async (topic) => { if (!window.confirm(`Delete “${topic.title}”?`)) return; try { await api.admin.deleteSafetyKnowledge(topic.id); await load(); } catch (requestError) { setError(requestError.message || 'Could not delete this knowledge item.'); } };
  const visible = topics.filter((topic) => [topic.title, topic.category, topic.slug].some((value) => value.toLowerCase().includes(query.toLowerCase())));
  return <div className="space-y-6"><div className="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><h1 className="text-2xl font-black tracking-tight text-slate-900 dark:text-white">Community Safety</h1><p className="mt-1 text-sm text-slate-500 dark:text-slate-400">Create and manage safety knowledge for community members.</p></div><button type="button" onClick={() => setEditing(null)} className="rounded-xl bg-[#012475] px-4 py-2.5 text-sm font-bold text-white shadow-sm dark:bg-[#4b9efe] dark:text-[#070d1e]"><i className="fa-solid fa-plus mr-2" />New knowledge</button></div><div className="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search knowledge…" className="w-full rounded-xl border border-slate-200 bg-white px-3.5 py-2.5 text-sm outline-none focus:border-[#4b9efe] dark:border-[#1e3568] dark:bg-[#0c1733] dark:text-white sm:max-w-md" /><span className="text-xs text-slate-500 dark:text-slate-400">{topics.length} total items</span></div>{loading ? <div className="rounded-2xl border border-slate-200 bg-white p-12 text-center text-sm text-slate-500 dark:border-[#1e3568] dark:bg-[#0c1733]">Loading Community Safety knowledge…</div> : null}{!loading && error ? <div className="rounded-2xl border border-red-200 bg-red-50 p-4 text-sm text-red-700">{error}</div> : null}{!loading && !error && !visible.length ? <div className="rounded-2xl border border-slate-200 bg-white p-12 text-center dark:border-[#1e3568] dark:bg-[#0c1733]"><i className="fa-solid fa-shield-halved mb-3 text-3xl text-slate-300 dark:text-slate-600" /><h2 className="text-base font-bold text-slate-800 dark:text-slate-200">No knowledge items found</h2><p className="mt-1 text-xs text-slate-400">Create a topic or adjust your search.</p></div> : null}{!loading && !error && visible.length ? <div className="grid gap-4 lg:grid-cols-2">{visible.map((topic) => <article key={topic.id} className="rounded-2xl border border-slate-200 bg-white p-5 dark:border-[#1e3568] dark:bg-[#0c1733]"><div className="flex items-start justify-between gap-3"><div><div className="flex flex-wrap items-center gap-2"><h2 className="m-0 text-base font-bold text-slate-900 dark:text-white">{topic.title}</h2><span className={`rounded-full px-2 py-1 text-[11px] font-bold ${topic.isPublished ? 'bg-emerald-100 text-emerald-700' : 'bg-slate-100 text-slate-600 dark:bg-slate-800 dark:text-slate-300'}`}>{topic.isPublished ? 'Published' : 'Draft'}</span></div><p className="mb-0 mt-1 text-xs font-semibold text-[#4b9efe]">{topic.category}</p></div><span className="text-xs text-slate-400">{dateLabel(topic.updatedAt)}</span></div><p className="mb-0 mt-3 text-sm leading-6 text-slate-500 dark:text-slate-400">{topic.shortDescription}</p><div className="mt-4 flex flex-wrap gap-2 border-t border-slate-100 pt-4 dark:border-[#1e3568]/60"><button type="button" onClick={() => setEditing(topic)} className="rounded-lg border border-slate-200 px-3 py-2 text-xs font-bold text-slate-700 dark:border-[#1e3568] dark:text-slate-200">Edit</button><button type="button" onClick={() => togglePublication(topic)} className="rounded-lg bg-[#eaf2ff] px-3 py-2 text-xs font-bold text-[#012475] dark:bg-[#132248] dark:text-[#4b9efe]">{topic.isPublished ? 'Unpublish' : 'Publish'}</button><button type="button" onClick={() => remove(topic)} className="rounded-lg px-3 py-2 text-xs font-bold text-red-600 hover:bg-red-50 dark:hover:bg-red-950/30">Delete</button></div></article>)}</div> : null}{editing !== undefined ? <TopicModal key={editing?.id || 'new'} allTopics={topics} topic={editing} onClose={() => setEditing(undefined)} onSave={save} /> : null}</div>;
}
